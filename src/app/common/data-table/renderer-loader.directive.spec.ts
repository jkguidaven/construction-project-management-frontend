import { RendererLoaderDirective } from './renderer-loader.directive';

describe('RendererLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RendererLoaderDirective();
    expect(directive).toBeTruthy();
  });
});

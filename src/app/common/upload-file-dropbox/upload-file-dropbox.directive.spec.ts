import { UploadFileDropboxDirective } from './upload-file-dropbox.directive';

describe('UploadFileDropboxDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFileDropboxDirective();
    expect(directive).toBeTruthy();
  });
});
